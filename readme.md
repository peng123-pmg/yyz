# 提交记录
2026/9/20 21：14
mainwindow.ui 是目前的主界面ui，后续应该会修改，
fandialog.ui  是风扇的ui界面，目前还很简陋，只绑定了风扇的开关函数到容器中，还没有实现ui中对应操控风扇的按钮功能
camera.ui       打算用来显示摄像头，目前只做了跳转功能，还没有实现具体ui界面

devicecontrol.c 和 devicecontrol.h有详细的注释，这个类就相当于一个控制中心函数，
把所有的硬件接口函数绑定到这个类中，然后通过这个类去操作硬件，而mainwindow.h 和 mainwindow.c 主要只负责ui显示

# 建议
1.像fan.h那样，把每个硬件都抽离成一个.h .c 文件，（风扇这个只有个.h文件夹是因为直接在.h文件中实现了，最好还是拆成.h和.c文件，好修改、扩展）

